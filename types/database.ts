// Este arquivo será substituído pelos tipos gerados pelo Supabase CLI:
// npx supabase gen types typescript --project-id <project-id> > types/database.ts
//
// Por enquanto, exporta um tipo vazio para que os clientes Supabase compilem.
export type Database = Record<string, never>;
